"""Standalone operational and demo scripts, run directly via ``uv run python``.

Kept in `mypy` and `ruff`'s scope from the start (see `pyproject.toml`) even
before any script exists, since `scripts/demo/*` is the PR template's
mandatory "How to validate" artifact.
"""
